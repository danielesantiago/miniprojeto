DROP TABLE IF EXISTS lance;
DROP TABLE IF EXISTS produto;
DROP TABLE IF EXISTS usuario;

CREATE TABLE usuario(
   id int PRIMARY KEY,
   nome varchar(50)
);

CREATE TABLE produto(
   codigo varchar(5) PRIMARY KEY,
   descricao varchar(50),
   preco float
);

CREATE TABLE lance(
   id_usuario int,
   codigo_produto varchar(5),
   valor_lance float,
   PRIMARY KEY (id_usuario, codigo_produto, valor_lance),
   FOREIGN KEY (id_usuario) REFERENCES usuario(id),
   FOREIGN KEY (codigo_produto) REFERENCES produto(codigo)
);

INSERT INTO usuario
VALUES
  (1, "Maia Allison"),
  (2, "Alvin Raymond"),
  (3, "Brock Langley"),
  (4, "Kyle Mayer"),
  (5, "Hilary Neal"),
  (6, "Fleur Cobb"),
  (7, "Simone Rice"),
  (8, "Knox Mcfarland"),
  (9, "Perry Fulton"),
  (10, "Kirestin Stevenson");

INSERT INTO produto 
VALUES
  ("VS001", "Vaso ornamental chinês", 140.80),
  ("MB001", "Smartphone Motorola Edge 50", 1200),
  ("MB002", "Smartphone Iphone 16 Pro Max", 3700.90),
  ("CD001", "Cadeira de Massagem Genis Relax", 800);

INSERT INTO lance
VALUES
  (1, "VS001", 150.80),
  (2, "VS001", 170),
  (1, "CD001", 840);
